import { StyleSheet } from 'react-native';
import ScaledSize from 'src/themes/sizes';

const styles = StyleSheet.create({
  headerContainer: {
    paddingHorizontal: ScaledSize.S_20,
  },
});

export default styles;
